package{

    public class Index extends EventDispatcher{


       
        
        public function Index()
        {

            var address:Object={
                name:"yejun"
            };

            const {name:String,post:*} = address;

        }
        
    }

}